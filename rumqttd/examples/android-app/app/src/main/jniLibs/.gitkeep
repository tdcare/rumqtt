# 请将编译好的 librumqttd.so 放到对应的 ABI 目录下：
#
# jniLibs/
# ├── arm64-v8a/librumqttd.so
# ├── armeabi-v7a/librumqttd.so
# ├── x86_64/librumqttd.so
# └── x86/librumqttd.so
#
# 使用 rumqttd/build_android.ps1 脚本编译后，
# 从 rumqttd/android-libs/ 目录复制对应文件到这里。
